import { GetSetIconPipe } from './get-set-icon.pipe';

describe('GetSetIconPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSetIconPipe();
    expect(pipe).toBeTruthy();
  });
});
